# 📋 下一步优化计划（v3）

> 基于 v1.7.1 评价修正。砍掉过度设计，聚焦"数据被真正使用"。
> **创建日期**：2026-06-23
> **最后更新**：2026-06-23
> **原则**：已有的数据要用起来，没用的要么用、要么删

---

## 一、已完成（本轮）

| 项 | 状态 | 备注 |
|:--|:-----|:-----|
| MEMORY.md 索引自动更新 | ✅ | |
| graph.json 覆盖增强 | ✅ | |
| access_count 闭环 | ✅ | |
| 快照自动清理（≤30个） | ✅ | |
| context 水印实测 | ✅ | |
| 旧 dispatcher + stackdump 清理 | ✅ | |
| PostToolUse 空 hook 移除 | ✅ | |
| confidence 分级 | ✅ | |
| self-discipline 规则去重 | ✅ | |
| learn-rules 反馈闭环 | ✅ | |
| recall 双阶段搜索 | ✅ | |
| **P0-1 recall 按 access_count 排序** | ✅ | `left-brain.sh` 已按 `access_count` 降序输出；`-sort` 参数开关未加，当前默认即按频次排序 |
| **P0-2 graph_search 2-hop** | ✅ | `graph-search.js` 已实现 hop1 + hop2 扩散 |
| **P0-3 修复 ROOT 快照索引** | ✅ | `save.js` 表头匹配正常，`test-snapshot-recovery.js` 9/9 通过 |
| **P1-5 .gitignore 补 stackdump** | ✅ | `.gitignore:5` 已含 `*.stackdump` |

---

## 二、下一步（按优先级）

### 🟡 P1：推 GitHub 获外部验证

| # | 任务 | 说明 | 阻塞 |
|:--|:-----|:-----|:-----|
| 4 | **创建 GitHub 仓库 + 推送** | `git remote add origin` + push，加 README badge | 你操作 |

---

## 三、不在计划内但应该顺手做的

| 项 | 触发条件 |
|:--|:---------|
| **snapshot 索引格式清理** | 已发现 `ROOT_快速加载会话.md` 有旧 `\| ⭐ \|` 格式残留，下次编辑时顺手统一 |
| **access_count 清理** | 如果 30 天后仍没有功能消费这个数据，考虑删掉字段减少复杂度 |
| **session-init 加载优化** | 当前每次启动加载 5 条 KB，可改为按相关性（而非时间）排序 |

---

## 四、路线图

```
现在 ── P0 已完成，P1-5 已完成
  ↓
下一步 ── P1（推 GitHub，需你操作）
  ↓
观察期 ── 用 2-3 天，看哪些功能实际被用到
  ↓
清理 ── 30天后删掉没用过的功能/字段
```

**核心思路**：不是继续加功能，而是**验证已有功能是否真的被用**。用 2-3 天后做一次"功能审计"——看 `access_count` 有没有 >0 的条目、graph_search 的 2-hop 是否比 1-hop 好、recall 排序是否改变了查看顺序。如果没变，说明这些功能是装饰性的，应该简化。

---

_v2 更新：砍掉 6 项低 ROI 任务，聚焦"数据用起来"和"外部验证"_  
_v3 更新：P0 全部完成，P1-5 完成，仅剩 P1-4 等用户推 GitHub_
