# 临时文件上传接口分析

## 接口地址

> /api/file/temp/upLoad

## ✅ 确认结果

**是的,`upLoadTempFile`方法确实是上传到向量库temp集合的方法。**

### 📊 完整调用链路

```
Controller.upLoadTempFile()
    ↓
FileInfoServiceImpl.upLoadTempFile()  (第1051行)
    ↓ 
1. addTempKnowledge() - 创建/获取temp知识库 (code="temp")
2. upLoad(fileFO) - 上传文件并设置isTemp=true
    ↓
parser() - 解析文件 (第962行)
    ↓
StateMachineEngine.execute(parserContext) - 状态机执行
    ↓
AbstractParserHandler.chunkUpload() - 上传到向量库 (第125行)
    ↓
关键代码 (第179-180行):
repo.set("db_name","tenant")
    .set("collection_name", getKnowLedgeCollectionCodeByFileCode(fileCode))
    
其中 fileCode = "temp" → collection_name = "temp"
    ↓
metadata中设置create_time (第155行):
metadata.set("create_time", System.currentTimeMillis())
    ↓
调用RAG API上传到向量库的temp集合
```


### 🔑 关键证据

1. **临时知识库code**: [`TEMP_KNOWLEDGE_CODE = "temp"`](file:///D:/工作资料/code/仓颉智能体/nlp-agent/agent-builder/agent-build-core/src/main/java/com/yundingtech/agent/build/modules/file/service/impl/FileInfoServiceImpl.java#L157)

2. **上传时设置temp标记**: [`fileFO.setIsTemp(true)`](file:///D:/工作资料/code/仓颉智能体/nlp-agent/agent-builder/agent-build-core/src/main/java/com/yundingtech/agent/build/modules/file/service/impl/FileInfoServiceImpl.java#L1065)

3. **向量库collection_name**: 通过[`getKnowLedgeCollectionCodeByFileCode(fileCode)`](file:///D:/工作资料/code/仓颉智能体/nlp-agent/agent-builder/agent-build-core/src/main/java/com/yundingtech/agent/build/modules/parser/execute/service/AbstractParserHandler.java#L231-L233)截取前4位,即"temp"

4. **metadata包含create_time**: [`metadata.set("create_time", System.currentTimeMillis())`](file:///D:/工作资料/code/仓颉智能体/nlp-agent/agent-builder/agent-build-core/src/main/java/com/yundingtech/agent/build/modules/parser/execute/service/AbstractParserHandler.java#L155)

### 🎯 结论

你的需求完全正确:**在`upLoadTempFile`方法执行完成后,清理向量库temp集合中不是今天的数据(根据metadata.create_time字段)**。