[KB-20260622-013] v1.3 三级检查点完成（plan + archive + save 集成）：
(1) plan-snapshot.js：阶段 1 计划快照（方案 + mermaid 图 + 文件路径清单）
(2) global-archive.sh：阶段 3 全局归档（拷贝 plan-* 快照 + 最近 10 个迭代快照 + Git tag archive-<任务>-<时间戳> + COMPLETION-REPORT.md）
(3) save.js：检测"完成/里程碑/交付"标签 + archives/ 目录存在 → 自动在启动段代码块加"💡 三级检查点提示"
使用流程：1) 开始任务跑 node scripts/parallel/plan-snapshot.js "标题" "描述" "mermaid" "files" 2) worker 跑完跑 node scripts/会话快照/save.js 3) 全部完成跑 bash scripts/parallel/global-archive.sh "标题"。archives/<任务>-<时间戳>/ 目录含完整快照索引 + 报告 + Git tag。