---
name: go
description: 自动完成 测试→简化→审查→提交 全流程（每阶段带验证标准）
---

执行完整交付流水线：

1. **测试** — 运行项目测试套件：
   ```
   # 验证: npm test 退出码 0，无失败用例
   ```

2. **简化** — 运行 /simplify 优化代码质量：
   ```
   # 验证: 简化后测试仍通过，无新增风格问题
   ```

3. **审查** — 运行 /code-review 审查：
   ```
   # 验证: 审查通过，无高风险问题未处理
   ```

4. **提交** — 如果所有检查通过：
   ```
   git add -A
   git commit -m "提交说明"
   # 验证: commit 成功创建
   ```

每个步骤失败则停止并报告原因，不继续执行。