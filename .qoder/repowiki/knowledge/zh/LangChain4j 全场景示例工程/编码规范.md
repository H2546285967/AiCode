- 统一使用 `LLMConfig` 配置类并通过 `@Bean` 方式注册 `ChatModel` 实例，集中管理 API Key、Base URL 及模型名称。
- 采用 `@AiService` 注解标记接口以声明式地定义 AI 助手，替代传统的命令式客户端调用代码。
- 在涉及外部数据源或工具调用的场景中，将具体逻辑封装为带有 `@Service` 或 `@Component` 注解的标准 Spring Bean，供 LLM 进行函数调用。
- 使用 Lombok 的 `@Data` 配合 LangChain4j 的 `@StructuredPrompt` 注解定义提示词模板类，实现参数化动态提示词生成。