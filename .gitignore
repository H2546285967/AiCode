# 临时文件
logs/
*.log
*.tmp
*.stackdump
.DS_Store
Thumbs.db

# 动态学习数据（每次运行变化，不应入库）
scripts/orchestrator/learned-keywords.json
data/workspace.db
data/github/

# 编辑器
.vscode/
.idea/
*.swp
*.swo

# 大文件（如果需要）
node_modules/
dist/
build/
target/

# 内部快照（本地会话状态，不进入 git，避免仓库膨胀）
.claude/snapshots/
.claude/skills/left-brain/memory/sessions/snapshots/

# 会话级快照模式覆盖（/snap-mode 写入，会话结束可清）
.claude/session-snap-mode.json

# Claude HUD 插件本地配置
.claude/settings.json
